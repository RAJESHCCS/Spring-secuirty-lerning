spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rajesh
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true # Enables SQL output to the console
        format_sql: true
